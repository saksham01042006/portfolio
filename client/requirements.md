## Packages
framer-motion | Complex animations and scroll reveals
react-icons | Social icons and UI elements
clsx | Class name utility
tailwind-merge | Class name utility

## Notes
Static images: import from @assets/logo.png for placeholders if needed, otherwise use Unsplash.
Font config: Tailwind config needs to support 'display' and 'body' fonts mapped to CSS variables.
